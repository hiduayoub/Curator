# Copy this file to `.env` and adjust as needed.
# The defaults below line up with the local docker-compose.yml services.

# PostgreSQL — used by Drizzle ORM.
DATABASE_URL="postgres://curator:curator@localhost:5432/curator"

# Redis — used by the BullMQ ingestion queue (Phase 3).
REDIS_URL="redis://localhost:6379"
